#include <string.h>

#include "morse.h"


char* string_to_morse( char* input_buf, int* error_code)
{

  char *conv_string = (char *)malloc(sizeof(input_buf) * 6);

  for (int i = 0; input_buf[i] != '\0'; i++) {
     


  }


}



char* morse_to_string( char* input_buf, int* error_code)
{

}


